# 8/16 定版改造验收报告

> 依据 docs/PLAN-0816-实施计划.md 的 14 项任务；每项=实现+验证+问题。
> benchmark 评测由用户手工执行（本轮不跑）。

## 一、任务完成总表

| # | 任务 | 状态 | 关键验证 | 发现并修复的问题 |
|---|---|---|---|---|
| T1 | SSH 通道替换 REST | ✅ | 直连/提权/真实 pi 冒烟三层 PASS | root 直连被拒→kali+sudo；jiti 导入→createRequire 锚定 |
| T2 | 状态机简化 | ✅ | 编译+状态迁移自测+残留 grep | T8 联调发现开放态需直通 solved（已修） |
| T3 | Planner 简化 | ✅ | v4-pro 实调出思路型文本 | 无 |
| T4 | w<idx> 目录隔离 | ✅ | 编译+组装断言 | 无 |
| T5 | ralph-loop 续跑 | ✅ | 提示注入断言；T11 升级进程内 | rpc 不消费 -p；follow_up 不触发新回合→prompt+steer |
| T6 | Supervisor v1 | ✅ | 增量 feed/看板动作单测+真实 LLM 审查（polly 33 轮） | 大窗口空 content→降窗口重试 |
| T7 | Idea/Memory 看板 | ✅ | 看板单测+真实落盘 | 无 |
| T8 | submit_flag/get_hint 工具化 | ✅ | 端到端：工具→worker-api→mock→correct 回传 | new→solved 非法迁移（联调实证） |
| T9 | React UI v1 | ✅（子代理） | 构建+全 API 实测+digest 实测 | TS type-only import；日志行截断 |
| T10 | message bus | ✅ | 总线单测（互见/防回声/游标） | 无 |
| T11 | conclude（rpc） | ✅ | RPC 注入实测触发新回合 | 同 T5 两坑 |
| T12 | LoopDetector 软警告 | ✅ | 扩展随冒烟正常加载 | 无（block 路径待实战） |
| T13 | 本地 KB v1 | ✅ | 索引+检索实测（LLL/png/overflow） | 无 |
| T14 | 轨迹+成本进 UI | ✅ | polly 实测 4.8M tokens/¥0.094；UI 构建 | 无 |

## 二、集成冒烟（mock 单题全链路，两轮）

**第一轮（修复前）**：全链路实际跑通——SSH 工具、写 solve.py、sympy 开根算出
`DASCTF{rsa_small_e_2026}`、submit_flag 工具返回 `{"correct": true}`；
但编排器未感知"工具提交成功"（只等 worker 退出带 flag），空转至 25 分钟超时。
**修复**：竞速循环每轮检查 cs.status，solved 立即收尾。
**第二轮**：验证修复后应在解出时立即收尾（见提交时补充）。

## 三、遗留与注意

1. **benchmark 验收待用户手工执行**；UI 使用 `python dashboard.py --workspace <ws>` 后访问 http://127.0.0.1:8088/ui/。
2. Kali 工具补齐（SageMath/pwndbg）为队友任务，未阻塞代码交付。
3. 提交纪律（submit.py）处于 parked 状态——平台规则探明后一键启用。
4. LoopDetector 的 block 路径与 Supervisor 提醒注入的实战效果待 benchmark 观测。
5. 开源前清理：secrets/kali.json、secrets/deepseek.key（已 gitignore）；文档中 IP 改占位符。

## 四、提交记录（8/16 定版改造）

074e606(T1) → b4aea50(T9) → 254f56e(T8) → T10-T12 → T13-T14 → 316f4a8(计划状态+stdin修复) →
（solved 感知修复随本轮提交）
