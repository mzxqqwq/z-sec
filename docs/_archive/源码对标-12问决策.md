# 源码对标：12 问决策（基于 Cairn / verialabs / Koshary / LLM-CTF-Solver / pi 五仓库逐文件证据）

> 证据来源：D:\ctf-agent\tmp\research-{cairn,verialabs,koshary-llmctf,pi}.md（子代理逐文件精读，全部带文件:行号）
> 本文件 = 每个问题「参考实现怎么解决 → 我们结合 3h DASCTF 赛制怎么做」的决策记录。
> 阅读顺序建议：先读 docs\系统架构说明.md（整体），再读本文件（每个模块的设计依据）。

---

## 1. triage：怎么判断题目难易

**参考实现**：
- Cairn：**没有 triage**。题目由人给 origin/goal，价值判断全靠 reason 的 LLM 语义规则
  （reason.md:30-39 "propose high-value and non-overlapping directions"，不是可计算分数）。
- verialabs：也没有。新题一律 spawn swarm 全模型竞速（无限预算哲学）。
- Koshary：**最接近**——`strategy_rank = points + 40*has_files + 25*kind_bonus - 30*failed_attempts`
  （orchestrator.py:1046-1057），纯规则打分决定求解顺序。
- LLM-CTF-Solver：有 `challenge_classifier` 做 LLM 题型分类（非难度）。

**我们的决策**：Koshary 公式打底 + LLM 难度评估 + 动态修正：
1. `rank = points*1.0 + 40*has_files + 25*kind_bonus - 30*failed_races`（移植 Koshary 公式，把 failed_attempts 换成 races）；
2. 加一层 LLM triage：描述 → `{difficulty 1-5, 理由}`，只在 points 缺失/同分时用；
3. 动态降级：races 失败后 rank 扣分而不是直接 dead（Koshary 的 `-30*failed_attempts` 语义就是"失败降权不放弃"——我们现在的"races 预算耗尽→dead"比参考实现更激进，3h 赛里应该改成"降权等待人工提示"）。

## 2. planning：模型与质量

**参考实现**：
- Koshary：**plan 复用求解同一个模型 CLI**（orchestrator.py:821 → generate_challenge_plan 的 runner_cmd），
  "You are a CTF Planning Agent" prompt → markdown 计划写 plan.md → `{plan}` 注入每轮 prompt；
  **无质量校验**，失败降级 "No plan generated."（orchestrator.py:677-708）。
- Cairn：无 planner，reason 任务承担"下一步方向"决策（分层监督，见 §5）。
- 我们现状：planning.py 用 `deepseek-chat`（便宜模型）——**这是我们对参考实现的偏离，不是继承**。

**我们的决策**（用户要求强模型，正确）：
1. `planner_routing`：easy→flash，medium/hard→v4-pro（或后续 GPT/Claude），配置进 l2-config.json；
2. 质量门禁：步骤 <3 或空话率过高 → 重生成一次（Koshary 没有，我们补）；
3. **失败重规划**（我们独有场景）：worker 僵局击杀时把失败摘要喂给 planner 重新出计划——参考实现都没有
   （它们要么无限重试要么靠人），3h 限时下这是我们止损的关键设计；
4. A/B 验证：`--no-planning` 开关 + benchmark 对照。

## 3. 模型路由与多模型接入

**参考实现**：
- Koshary：静态路由表（config.json:68-89：web/forensics/mobile→gemini、crypto/reverse/misc→claude、
  pwn→codex），`choose_route` 子串匹配（orchestrator.py:214-219）→ 细化 model_key → runner+prompt 模板。
- verialabs：无路由——每道题 5 个模型全上竞速（models.py:21-27）。
- Cairn：worker 配置 task_types/max_running/priority（config.py:176-184）。

**我们的决策**：Koshary 路由（静态、零成本、可预期）为主 + verialabs 竞速（限 2 路）为辅 +
Cairn 优先级字段（强模型 priority 低数字先派）。
pi 侧接入（research-pi §4）：models.json `providers.<id>.{baseUrl,apiKey,api,models[]}`，
OpenAI=`api:"openai-completions"`+`$OPENAI_API_KEY`，Claude=`api:"anthropic-messages"`+`$ANTHROPIC_API_KEY`，
key 用 `$ENV` 占位符。等用户给 key 即接入。

## 4. 执行链路与工具链

**参考实现**：
- Cairn `local_backend.py`：**无 Docker 时的官方参考实现**——ensure_running=建
  `<workspace_root>/<project_id>/` 目录，子进程执行（我们 Kali REST 的形态等价物）。
- LLM-CTF-Solver `ssh_client.py`：SSH 通道打 Kali（我们 REST 通道的兄弟方案）。
- verialabs 沙箱工具清单（sandbox-tools.txt:210-215）含 **SageMath、flatter、cado-nfs、RsaCtfTool、
  volatility3、tesseract**——对照清单发现我们缺 SageMath（已确认）与 flatter。

**我们的决策**：链路不变（官方 API→Windows 编排器→pi worker→kali.ts→Kali REST→Kali）。
工具链补齐优先级：SageMath（格/多项式题刚需）> pwndbg > RsaCtfTool/flatter。
并发：Kali API 是 Flask threaded，双 worker 已实测无冲突；真正的并发风险在同目录互踩（见 §11）。

## 5. 监督模块（benchweava 思路的源码实证）

**参考实现**：
- **Cairn reason 任务 = 最完整的分层监督**（research-cairn §3）：只读图 YAML 快照 + open intents，
  输出 `complete/intents/noop` 三种动作；靠 `create_intent` 给 explore 供料、`complete` 收束项目；
  触发去重靠 ReasonCheckpoint 记录 fact/hint/intent 三个计数（loop.py:704-718）。
- **verialabs coordinator = 最轻量的监督**（coordinator_core.py:133-171）：读每 solver JSONL 尾部
  last_n 行折叠成可读摘要 → bump 注入具体指引；人消息走 operator_inbox 由 coordinator LLM 转译。

**我们的决策**：合成两者的轻量版 supervisor.py：
1. 每 5 分钟读 worker 事件流尾部 → 强模型输出 `{verdict: on_track|off_track|stuck, correction}`；
2. `off_track` → 注入纠偏；`stuck` → 杀 + 失败摘要重规划（与 §2 联动）；
3. 注入通道：pi 扩展 `pi.sendUserMessage({deliverAs:"steer"})`（research-pi §6 已证实可行）；
4. 成本控制：只对 medium/hard 开监督；easy 靠 flash 裸跑。
（benchweava 源码等用户提供后按同法拆解，再修订本节。）

## 6. 提交纪律（冷却/去重/上限）

**参考实现**：
- verialabs（swarm.py:152-192，逐行已核验）：`[0,30,120,300,600]`s，**按模型计数**（_submit_count
  的 key 是 model_spec）、跨模型精确去重、asyncio.Lock 串行；**无硬上限**，冷却封顶 600s 永不禁赛；
  `already_solved` 也算确认（防同队重复提交）。
- Koshary：**完全没有冷却/限流/重试**，只有 attempted_flag 去重 + max_wrong 硬上限
  （HTB 默认 3、CTFd 默认 0=不限；**异常提交也计数**，orchestrator.py:968-978）。
- Cairn：零纪律，只有 skill 提示词软约束"高置信才提交"。

**我们的决策**：
1. 冷却表参数化（l2-config）+ **改成按模型计数**（修正现状的按题计数——verialabs 的证据表明
   一个坏模型不该拖累另一个模型的提交节奏）；
2. 保留 max_wrong=3 硬上限但改为"看板一键解锁"（比 verialabs 严、比 Koshary 松——DASCTF 未探明前保底防封号）；
3. 8/18 测试赛用 probe 脚本实测平台限流真实参数，回来只改配置数字。
（教训：三个参考实现的提交纪律天差地别，说明平台规则决定一切——没有实测就没有正确答案。）

## 7. 本地知识库

**参考实现**：
- LLM-CTF-Solver：ChromaDB RAG（我们早期评估已砍）+ 三层记忆（12 类受保护事实防丢，memory.py:16-29）。
- Cairn：facts/intents 因果图当长期记忆 + "把已排除方向写成 Hint 给其他 worker"（server-protocol.md:95）。
- verialabs：message bus 短期共享（见 §12-1）。

**我们的决策**：两级知识：
1. **短期共享（先做）**：移植 verialabs message bus（54 行全套代码已有）——同题双 worker 共享发现；
2. **长期库（后做）**：本地 KB 服务（Windows :8099）+ `kb_search` 工具（kali.ts 扩展注册），
   数据 = 330 题官方 solver/writeup 提炼"题型特征→手法→关键命令"三元组 + Exploit-DB；
   清洗：按题型打标、去 flag 噪音、只留可复用模式；benchmark 评测时开关关闭（完整性铁律）。

## 8. 人机回路

**参考实现**：
- verialabs：operator POST /msg → inbox → coordinator LLM 决定怎么 bump（coordinator_loop.py:233-280）；
  trace 摘要 = JSONL 尾部 last_n 行折叠（coordinator_core.py:146-167）。
- Cairn：hints `{id,content,creator,created_at}`（**无 version/ack**，models.py:32-37）；
  "重规划"= hint_count 变化触发 reason 重跑（loop.py:704-718）；flag 错误的反馈走 reopen 通道。
- LLM-CTF-Solver：UserInterface 抽象 + 每步三选一（批准/反馈重想/终止）+ confirm_flag_callback
  人工确认 flag + reflection 基于历史续想（solve_agent.py:674-739）。

**我们的决策**：digest.py（LLM 把事件流尾部翻译成 3 行中文摘要 + 卡点原因）→ 看板展示；
hints 落 state.json（加 scope/priority 字段，版本=计数）；**hint 版本变化触发重规划**
（移植 Cairn _reason_trigger 的计数快照模式）；verify 模式移植 confirm_flag_callback。

## 9. postmortem 定位

确认：仅赛前训练 + 8/18 测试赛后复盘用，正式比赛不跑。参考实现都没有赛后复盘模块——
这是我们"训练闭环"赛点（官方考察点）的自研内容。

## 10. 黑板与 BasePlatform

**参考实现**：
- Koshary StateDB（orchestrator.py:467-569）：单 JSON 五块（solved_ids/submitted/challenges/errors/stats），
  全部写操作包全局 STATE_LOCK；每题 `chdir/.lock` 用 O_CREAT|O_EXCL 原子抢占（orchestrator.py:661-671）。
- BasePlatform（platforms/base.py:71-126）：NormalizedChallenge + SubmitResult + 4 抽象方法 +
  实例生命周期默认 no-op——我们 platform.py 已按同构实现（这就是问题 10 的答案：黑板和适配器
  都不是发明，是 Koshary 模式的等价重写）。

**差距（可补）**：Koshary 每题 .lock 文件我们没做——单编排器进程下暂不需要，但若以后
并行编排器进程，直接抄 O_EXCL 模式。

## 11. 隔离：容器 vs 软隔离

**参考实现**：
- Cairn：**每项目一个容器**（cairn-dispatch-<project_id>，sleep infinity 常驻，containers.py:31-72）；
  文件 put_archive tar 注入无 bind mount；限资源只有 timeout -k + cap_add + network_mode，
  **连 Cairn 都没有内存/CPU 硬配额**（research-cairn §7 明确）；另有 local_backend 无容器版。
- verialabs：**每模型一个容器** + 每容器唯一 mkdtemp 目录绑 /challenge/workspace（rw）、
  distfiles ro、16GB 内存 + 2 CPU（sandbox.py:109-149）；exec 包 timeout --signal=KILL。
- LLM-CTF-Solver：SSH + 本地 AST 沙箱（python 工具）。

**我们的决策**（用户 VPN 可解 registry）：
1. **8/18 前（必须）**：软隔离 = `/root/ctf/<cid>/w<idx>/` 每 worker 独立目录（≈Cairn local_backend 模式）
   + 现有进程组杀 + 命令 timeout——这是"无容器官方实现"给的底气，够解静态题；
2. **VPN 通后**：容器优先用于**服务类题靶机**（cybench/CTFTiny 自带 docker-compose，这是容器不可替代处）；
3. **worker 全容器化**排在服务题之后（API 改造 + 镜像构建回归风险 > 收益，8/21 前不动）。

## 12. 欠账清单落地计划（每项都有源码出处与实现路径）

| # | 项 | 出处 | 实现路径（我们环境） | 时点 |
|---|---|---|---|---|
| 1 | message bus | verialabs message_bus.py（54 行全套） | 落盘文件版总线 + kali.ts 注册 check_findings 工具（进程内游标） | 8/18 前 |
| 2 | w<idx> 目录隔离 | Cairn local_backend + verialabs mkdtemp | 编排器 remote_root 加 w<idx>，kali.ts 拼路径 | 8/18 前 |
| 3 | 规划器强模型+失败重规划 | Koshary plan 复用求解模型 + 自研失败重规划 | planning.py 路由化 + stuck 击杀时带失败摘要重规划 | 8/18 前 |
| 4 | 提交策略参数化+按模型计数 | verialabs swarm.py:152-192 | submit.py 改造 + l2-config 配置表 | 8/18 前 |
| 5 | digest+看板升级 | verialabs coordinator_core.py:133-171 | digest.py + dashboard.py | 8/18 前 |
| 6 | LoopDetector | verialabs loop_detect.py（12 窗/3 警/5 断） | 写成 pi 扩展（-e loop-detect.ts）或并入 kali.ts | 8/18 前 |
| 7 | Supervisor | Cairn reason 任务 + verialabs coordinator | supervisor.py + pi sendUserMessage(steer) 注入 | 8/19 |
| 8 | 双阶段 conclude | Cairn explore.py:241-384 + pi abort/followUp | workers 改 --mode rpc：deadline-90s 发 abort+follow_up 总结 | 8/19-8/20 |
| 9 | 结构化输出约束 | verialabs output_types.py | pi 无 output schema：提示词硬约束 + 编排器终检（flag 正则）两层 | 8/19-8/20 |
| 10 | JSONL 轨迹+成本 | verialabs tracing.py + pi message_update.usage | 从现有事件流派生 trace 摘要 + cost 聚合 | 8/19-8/20 |
| 11 | 本地 KB v1 | LLMCTF RAG 思路 + 自研清洗 | kb_server.py + kb_search 工具 + 评测隔离开关 | 8/19-8/20 |
| 12 | Kali task_id/cancel | Cairn 协议层（claim/heartbeat/release） | 暂缓：同步 REST + 客户端超时 + 进程组杀已覆盖需求；conclude 走 pi 侧 abort | 8/21 后评估 |
| 13 | 服务题容器（VPN） | cybench/CTFTiny docker-compose | podman 起靶机；worker 容器化后置 | 8/18 观察后定 |
| 14 | SageMath/flatter/pwndbg | verialabs sandbox-tools.txt 对照 | 队友装 Kali | 8/18 前 |

**原则**：1-6 在 8/18 测试赛前落地（测试赛=验收+探平台）；7-11 用 8/19-8/20 两天落地；
8/18 当天唯一新增任务是平台规则探测。每项都带 benchmark A/B 验证方式，不空跑。

---

## 13. BreachWeave 增量修订（2026-08-16 源码研究后）

> 依据：docs/analysis/research-breachweave.md（腾讯黑客松第二期 1/613 冠军，Manager/Solver/Observer）。

### 13.1 对 §5（监督模块）的升级——Observer 设计全参数照抄

BreachWeave 的 Observer = 冠军方案对"监督者"的完整答案，且与我们计划同构（旁路监督+轻量纠偏+不杀），
参数与机制直接采纳：
- **触发节奏**：每 6 个 assistant 轮次（不是固定分钟）或 hint 获取或 agent_end 时审查一次，
  每次只回看最近 10 轮（observer-loop.ts:9-13,359-365）；
- **输入**：最近 10 轮压缩轨迹（每轮 = assistant 摘要 + 工具日志只留 name/args160字/result160字/is_error）；
- **独立会话**：Observer 用独立 session 目录 + 可单独指定模型（observerModel），与 Solver 物理隔离；
- **输出契约**：无强 JSON schema，系统提示约束"无改动回 NO_CHANGE；有改动 1-4 条短 bullet"，
  动作经工具落地（memory_*/idea_*/send_efficiency_reminder）；
- **纠偏**：`pi.sendUserMessage(..., {deliverAs:"steer"})`，从不终止 Solver；
- **防刷屏**：efficiency reminder 带冷却（6 轮）+ 指纹去重（同消息/同活动模式不重发，observer-loop.ts:72-106）。
- **我们的落地形态**：我们没有 pi SDK 进程内扩展，supervisor.py 在编排器侧读 worker 事件流，
  节奏对齐"每 6 轮工具调用"（stuck.py 已有轮次计数基础）；纠偏注入走 worker 提示词下一轮
  （当前架构）或后续切 --mode rpc 后走 steer（8/19 conclude 改造时一并做）。

### 13.2 Idea/Memory 双层看板（§7/§8 的上位设计）

- **Idea**（方向假设，生命周期 pending/testing/verified/failed/skipped，lowercase 归一化去重）
  与 **Memory**（fact/evidence/failure/hint 四类事实）分开维护（memory.ts:4-34）；
- 权限：**Observer 写、Solver 只读、Manager 落盘 challenge 级**；新 Solver 启动时把 challenge 级
  看板 seed 进自己的板（manager.ts:1198-1207）；
- 体积硬上限：memory ≤12、ideas ≤8，进 prompt 截断到 220/120 字（manager.ts:62-66）；
- **我们的落地**：state.json 每 cid 加 `board: {ideas[], memory[]}`；supervisor.py 写、worker 提示词
  注入只读摘要；这是 message bus（短期共享）与 KB（长期库）之间的"中程状态层"，优先级排在
  message bus 之后、KB 之前。

### 13.3 结束条件外置 + 续跑循环（直接抄 ralph-loop）

- 完成判定唯一信号：`flag_count>0 && flag_got_count>=flag_count`（store.ts:263-266）——平台 API 的
  flag 计数，模型无权宣布结束；
- **agent_end 续跑**：worker 回合结束但题目未完成 → 注入"继续当前任务，不要重复已完成步骤，
  多 flag 题不要交对一个就停，直到比赛 API 明确显示完成"（ralph-loop.ts:7-8,75-85）；
- 回收门槛：题目 stale（超时且无正确提交）前禁止释放 Solver（manager.ts:1432-1435）；
- **我们的落地**：worker 结束未解时自动续跑一次（预算内），多 flag 题语义加入提示词模板；
  "是否完成"以平台返回为准——8/18 探测 DASCTF 是否有 flag 计数端点，没有则用"该题 solved"
  代替。这是 v0.11 复测里 flash worker 交对一个就停问题的对症药，**排在 8/18 前第 5 位**。

### 13.4 提交走 host-bridge 工具（§6 的架构修正）

BreachWeave 的提交是 Solver 内工具 `challenge_submit_flag` → host-bridge RPC → Manager 统一提交
（api-client.ts:120-128）——worker 显式调工具，纪律在中心。**我们改**：kali.ts 注册
`submit_flag`/`get_hint` 工具，经 Windows 本地 HTTP 回调编排器（新端点 :8089/worker-submit），
编排器统一走 SubmissionPolicy——worker 不再靠文本正则交 flag，纪律 100% 中心化
（同时保留正则抽取作兜底）。这比 verialabs 的纯正则拦截更干净。时点：8/18 前（与提交纪律改造同一批）。

### 13.5 BreachWeave 没有的，恰好验证我们的保留项

- 无 message bus（Manager 中心 steer/follow_up 广播替代）→ 我们仍做轻量文件版总线（我们的
  worker 无 SDK 通道，广播不如总线方便）；
- 无 flag 去重/冷却（平台无惩罚 + 客户端 3req/s 限速）→ 我们保留纪律（DASCTF 规则未探明前保底）；
- 无僵局检测（只有 stale 超时）→ 我们保留 stuck.py（3h 预算经不起空转）；
- triage 弱化（LLM Planner + 粗排序 untouched→difficulty→remainingScore）→ 我们采用其预排序
  三键序（未动过→难度→剩余分），但保留 Koshary 公式与 LLM 难度评估。

### 13.6 可后续抄的储备件

- `security_kimi_search`（Kimi+Qwen 双源联网安全检索工具）→ 我们的 kb_search 工具设计参考；
- `largeToolResultExtension`（>32k 工具结果落盘，上下文只留 600 字预览+分段读指引）→ 我们的
  Kali 大输出场景直接受益，8/19 顺手做；
- `pentestCompactionExtension` 的 MUST-preserve 清单（scope/assets/hypotheses/findings/auth/evidence/
  phase/next）→ 我们重规划摘要生成器的保留清单（已部分对齐 LLMCTF 受保护事实）。
