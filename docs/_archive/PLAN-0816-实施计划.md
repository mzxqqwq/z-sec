# 8/16 实施计划（定版方案落地执行表）

> 依据：docs/定版方案-最终.md（用户 2026-08-16 定稿）
> 执行纪律：**每个任务 = 实现 → 简单验证 → 向用户汇报（完成情况 + 遇到的问题）**。
> 全部任务今天（8/16）完成，随后跑 mock 回归 + benchmark 验收。
> 状态标记：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⚠️ 受阻

---

## 0. 总目标

把系统从"调度换题型"改造为"AK 引导解题型"：SSH 通道、简化状态机、Observer 监督、
续跑防早停、React 看板、KB 检索，全部落地并跑通 benchmark。

---

## 1. 任务清单

### T1 SSH 通道替换 REST ✅
- **改动**：`src\pi-ext\kali.ts` 重写为 ssh2 直连（`npm install --prefix src\pi-ext ssh2`）；
  连接复用 + 命令超时 + 断线重连；凭据 `secrets\kali.json`（kali 用户 + sudo -S 提权，root 直连被 sshd 拒）。
- **保留**：Kali REST `/health` 健康闸门不动。
- **验证**：node 直连/提权/真实 pi 冒烟三层 PASS（提交 074e606）。
- **问题与解决**：①root 直连被拒 → kali+sudo 提权；②jiti 解析不到扩展目录 node_modules → createRequire 锚定。

### T2 状态机简化 ✅
- **改动**：删 triage 排序/races 预算/dead/僵局击杀/提交纪律（submit.py parked）；
  状态流转 new→solving→solved/needs_hint；`_submit_direct` 直接提交；旧 dead 数据映射 needs_hint。
- **验证**：编译 + 状态迁移自测 + 残留 grep 干净。
- **问题**：无（后续 T8 联调发现"worker 可随时交卷"需开放态直通 solved，已修）。

### T3 Planner 简化 ✅
- **改动**：总体思路 prompt（核心方向+2-3 要点，不列工具步骤）；模型 v4-pro；删门禁/重规划。
- **验证**：实调 RSA 题输出思路型文本（gmpy2.iroot 整数开根、浮点开根易错点）✅。
- **问题**：无。

### T4 w<idx> 目录隔离 ✅
- **改动**：每 worker 独立远程目录 /root/ctf/<cid>/w0、w1，附件各自同步。
- **验证**：编译 + 提示词组装断言。
- **问题**：无。

### T5 ralph-loop 续跑（编排器层 v1 → T11 升级为进程内） ✅
- **改动**：提示词契约（多 flag 别停/别过早放弃）；续派注入【续跑提示】；连续未解超预算停 needs_hint。
  **T11 后升级**：agent_end 事件 → stdin prompt 注入强制新回合（真正进程内续跑）。
- **验证**：提示注入断言 + RPC 注入实测（模型回"第二"）。
- **问题**：rpc 模式不消费 -p、follow_up 不触发新回合（改 prompt+steer），已修。

### T6 Supervisor v1（BreachWeave Observer 移植） ✅
- **改动**：`supervisor.py`——增量日志 feed（字节偏移）；每 6 轮审查（race 结束强制一次）；
  10 轮窗口压缩轨迹；系统提示词全文移植（三步闭环/三连自问/提醒四前提/体积上限）；
  纠偏=提醒注入下一轮提示词（不杀 worker）；冷却+指纹去重。
- **验证**：增量 feed 单测、看板动作单测、真实 LLM 审查（polly 33 轮 → 高质量 evidence）。
- **问题**：大窗口输入 v4-pro 偶发空 content → 降窗口重试（10→5 轮），已修。

### T7 Idea/Memory 看板 ✅
- **改动**：state.json `board:{ideas[],memory[]}`；Supervisor 写、worker 只读（提示词注入摘要，
  ideas≤8×120 字、memory≤12×220 字）；UI /api/board 已支持。
- **验证**：看板动作落地/去重/上限单测 + 真实审查后落盘。
- **问题**：无。

### T8 submit_flag/get_hint 工具化 ✅
- **改动**：kali.ts 注册两工具 → worker-api(:8089) → 编排器统一提交；派工加 --cid；
  BasePlatform.get_hint；提示词优先用工具、正则兜底。
- **验证**：端到端实测（pi 调工具 → mock 判题 → `{"correct": true}` 回 worker）。
- **问题**：正确 flag 触发 new→solved 非法迁移（AssertionError 断连）→ 开放态直通 solved，已修。

### T9 React UI v1 ✅（子代理交付）
- **改动**：ui/（Vite+React+TS）+ dashboard.py 8 个 JSON API + /ui 静态 + digest.py 中文摘要；
  题目列表/详情/board/hint 输入/verify 确认。
- **验证**：构建 + 全 API curl 实测 + digest 真实日志产出合格摘要（b4aea50）。
- **问题**：TS verbatimModuleSyntax type-only import；超大日志行 → 服务端截断。均已解决。

### T10 message bus（文件版） ✅
- **改动**：`message_bus.py`（verialabs 移植落盘版）+ 编排器投递 worker 最终摘要 +
  kali.ts `check_findings` 工具（进程内游标、排除自己）；提示词"每几步查一次"。
- **验证**：总线单测（互见/不见自己/已读不回传/游标平移）全过。
- **问题**：无。

### T11 conclude（双阶段收尾，pi rpc） ✅
- **改动**：worker 切 --mode rpc（stdin 双向）；初始任务经 stdin prompt 下发；
  agent_end 未解 → 注入续跑 prompt；deadline-90s → abort + 总结 prompt（90s 收尾，成果不丢）。
- **验证**：RPC prompt+steer 注入实测触发新回合。
- **问题**：见 T5 备注（两个坑已修）。

### T12 LoopDetector 软警告 ✅
- **改动**：`loop-detect.ts` 扩展（12 窗/3 警/5 断；≥3 steer 警告带指纹去重、≥5 tool_call block）；
  挂进 DEFAULT_PI_CMD。
- **验证**：随 rpc 冒烟正常加载；block 路径待 benchmark 实测。
- **问题**：无。

### T13 本地 KB v1 ✅
- **改动**：`kb_server.py`(:8099 只读检索，索引 330+ 题 + writeup + 题型手法提示)；
  kali.ts `kb_search` 工具；评测隔离（KB_ENABLED 默认 0）；编排器 kb_enabled 时懒启动服务。
- **验证**：索引启动 + 检索实测（LLL/png/overflow 均返回带手法提示的结果）。
- **问题**：无。

### T14 轨迹 + 成本进 UI ✅
- **改动**：`tracing.py`（usage 聚合）+ dashboard /api/usage + /api/state 带 tokens/cost
  （mtime 缓存）+ UI 成本列（title 显示 token 数）。
- **验证**：真实 polly 日志聚合 4.8M tokens/¥0.094 ✅；UI 构建通过。
- **问题**：无。

---

## 2. 依赖与执行顺序

```
T2 → T3 → T4 → T5    编排器核心（我本人串行改）
        ↓
T6 → T7              监督与看板（依赖 T2）
T1（SSH）与 T9（UI）可并行（fork 子代理各负责一条）
T8 依赖 T1+T2；T10-T14 依赖 T5/T6 完成
顺序建议：先 T2/T3/T4/T5 主体，T1/T9 后台并行，T6/T7 随后，T8 接 T1，最后 T10-T14。
```

## 3. 验收（全部完成后）

1. mock 平台 4 题全链路（SSH 通道 + 双 worker + supervisor + UI 可见）；
2. benchmark 复测：CTFTiny polly 等单题 + L1 easy 回归；
3. 产出验收报告：每任务验证结果 + 遇到的问题 + 遗留项。

## 4. 环境速查（执行用）

- 编排器：`D:\ctf-agent\src\ctf_orchestrator\`（ctf_orchestrator.py / state.py / planning.py / workers.py / submit.py(parked)）
- pi 扩展：`D:\ctf-agent\src\pi-ext\kali.ts`；pi CLI：`D:\ctf-agent\pi-mono\packages\coding-agent\dist\cli.js`
- 启动器：`D:\ctf-agent\src\run-pi.ps1`（手动调试用；编排器直连 node）
- 模型：v4-pro（强）/ v4-flash（弱），models.json：`C:\Users\86173\.pi\agent\models.json`
- Kali：10.174.153.128:22（SSH root）、:5000（REST /health 保留）；凭据 `D:\ctf-agent\secrets\kali.json`
- 端口：mock 7788 / dashboard+UI 8088 / worker-api 8089 / KB 8099
- 工作区：workspace（比赛）/ eval-workspace（评测）；benchmark 题库：benchmarks\ctftiny、nyu-ctf-bench、cybench
- 历史验证命令：`python src/ctf_orchestrator/eval_run.py --platform ctftiny --only cry-polly-crack-this --config src/ctf_orchestrator/l2-config.json`
